{
  "name": "@pnpm-monorepo/ui",
  "version": "0.0.1",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/*.js src/components/*.jsx --outdir=dist --target=chrome95",
    "dev": "esbuild src/*.js src/components/*.jsx --outdir=dist --target=chrome95 --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "marker dao",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.13.11"
  },
  "peerDependencies": {
    "react": "^18.2.0"
  },
  "dependencies": {
    "@emotion/css": "^11.5.0"
  }
}
